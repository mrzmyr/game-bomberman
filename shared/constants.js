const EXPLOSION_TYPE_TOP = 1;
const EXPLOSION_TYPE_LEFT = 2;
const EXPLOSION_TYPE_RIGHT = 3;
const EXPLOSION_TYPE_BOTTOM = 4;
const EXPLOSION_TYPE_CENTER = 5;

if(typeof module !== 'undefined' && module.exports) {
  module.exports = {
    EXPLOSION_TYPE_TOP,
    EXPLOSION_TYPE_LEFT,
    EXPLOSION_TYPE_RIGHT,
    EXPLOSION_TYPE_BOTTOM,
    EXPLOSION_TYPE_CENTER
  }
}